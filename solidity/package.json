{
	"type": "module",
	"scripts": {
		"contracts": "bun install --frozen-lockfile && bun run compile && cd .. && bun run vendor",
		"compile": "bun ./ts/compile.js"
	},
	"license": "ISC",
	"devDependencies": {
		"@types/node": "22.13.10",
		"solc": "0.8.30"
	}
}
